% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regulog.R
\name{log_action}
\alias{log_action}
\title{Log a discrete action in the audit trail}
\usage{
log_action(log, action, object, reason, user = log$user)
}
\arguments{
\item{log}{A \code{regulog} object from \code{\link[=regulog_init]{regulog_init()}}.}

\item{action}{Character. What happened (e.g. \code{"approved"}, \code{"deployed"},
\code{"rejected"}, \code{"exported"}).}

\item{object}{Character. What it happened to (filename, model ID, record
ID, pipeline step, etc.).}

\item{reason}{Character. \strong{Mandatory.} Why it happened. No default.}

\item{user}{Character. Override the session user for this entry. Defaults
to the user set at \code{\link[=regulog_init]{regulog_init()}}.}
}
\value{
The \code{regulog} object, invisibly (pipe-friendly).
}
\description{
Records a user action (approval, rejection, sign-off, deployment, export,
etc.) as a tamper-evident, hash-chained entry in the audit log.
}
\examples{
log <- regulog_init(app = "my-app", user = "jsmith")
log_action(log,
  action = "approved",
  object = "model_v3",
  reason = "Validation metrics passed agreed threshold"
)

}
