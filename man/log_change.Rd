% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regulog.R
\name{log_change}
\alias{log_change}
\title{Log a before/after field change in the audit trail}
\usage{
log_change(log, object, field, before, after, reason, user = log$user)
}
\arguments{
\item{log}{A \code{regulog} object from \code{\link[=regulog_init]{regulog_init()}}.}

\item{object}{Character. The record being modified (e.g. \code{"user_42"},
\code{"config.yaml"}, \code{"experiment_7"}).}

\item{field}{Character. The field that changed (e.g. \code{"status"}, \code{"threshold"}).}

\item{before}{The value before the change (coerced to character).}

\item{after}{The value after the change (coerced to character).}

\item{reason}{Character. \strong{Mandatory.} Why the change was made. No default.}

\item{user}{Character. Override the session user. Defaults to session user.}
}
\value{
The \code{regulog} object, invisibly.
}
\description{
Records a data modification with both prior and new values. Use this
whenever a specific field on a record is changed and you need a full
history of what it was, what it became, and why.
}
\examples{
log <- regulog_init(app = "my-app", user = "jsmith")
log_change(log,
  object = "experiment_7",
  field  = "learning_rate",
  before = "0.01",
  after  = "0.001",
  reason = "Loss diverging at 0.01 — reduced per tuning protocol"
)

}
