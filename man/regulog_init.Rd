% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regulog.R
\name{regulog_init}
\alias{regulog_init}
\title{Initialise a regulog audit log session}
\usage{
regulog_init(
  app,
  version = "unknown",
  user = Sys.info()[["user"]],
  path = NULL,
  hash_algo = "sha256"
)
}
\arguments{
\item{app}{Character. Application or system name (e.g. \code{"data-pipeline"},
\code{"review-tool"}, \code{"ml-trainer"}).}

\item{version}{Character. Application version string.}

\item{user}{Character. Identity of the acting user. Defaults to
\code{Sys.info()[["user"]]}. In Shiny, pass \code{session$user}.}

\item{path}{Character or \code{NULL}. Path for persistent storage. If \code{NULL},
the log is in-memory only (suitable for development / testing).}

\item{hash_algo}{Character. Algorithm passed to \code{\link[digest:digest]{digest::digest()}}.
Defaults to \code{"sha256"}. Do not change once a log file is in use.}
}
\value{
An S3 object of class \code{"regulog"} (an environment).
}
\description{
Creates a new audit log session object. Subsequent calls to \code{\link[=log_action]{log_action()}}
and \code{\link[=log_change]{log_change()}} append hash-chained entries. If \code{path} is supplied,
entries are written to a newline-delimited JSON file (\code{.rlog}).
}
\details{
\subsection{Entry structure}{

Every entry written to disk is a JSON object on a single line:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "entry_id":    1,
  "timestamp":   "2026-06-18T14:32:01.123456Z",
  "app":         "my-app",
  "app_version": "1.0.0",
  "user":        "jsmith",
  "action":      "approved",
  "object":      "model_v3",
  "reason":      "Validation metrics passed threshold",
  "prev_hash":   "e3b0c44298fc1c149afb...",
  "entry_hash":  "a87ff679a2f3e71d9181..."
\}
}\if{html}{\out{</div>}}

The flat structure is intentional: the log should be inspectable with a
text editor, without any specialist software.
}

\subsection{Hash chain}{

Each \code{entry_hash} is SHA-256 of a canonical string encoding all fields
plus \code{prev_hash}. Altering any field — including the timestamp or reason —
invalidates the hash and all subsequent chain links, detectable by
\code{\link[=verify_log]{verify_log()}}.
}

\subsection{What the chain captures}{\tabular{ll}{
   Property \tab Implementation \cr
   Who acted \tab \code{user} field on every entry \cr
   What happened \tab \code{action} + \code{object} fields \cr
   When \tab ISO-8601 UTC timestamp, microsecond resolution \cr
   Why \tab Mandatory \code{reason} — no default \cr
   What changed \tab \code{before}/\code{after} in \code{\link[=log_change]{log_change()}} \cr
   Tamper evidence \tab SHA-256 hash chain; verified by \code{\link[=verify_log]{verify_log()}} \cr
   Portable export \tab \code{\link[=export_audit_trail]{export_audit_trail()}} to CSV or JSON \cr
}

}
}
\examples{
log <- regulog_init(
  app     = "my-app",
  version = "1.0.0",
  user    = "jsmith"
)
log

}
