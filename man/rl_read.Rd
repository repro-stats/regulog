% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{rl_read}
\alias{rl_read}
\title{Log a data read operation}
\usage{
rl_read(log, reader, ...)
}
\arguments{
\item{log}{A \code{regulog} object from \code{\link[=regulog_init]{regulog_init()}} or \code{\link[=regulog_shiny_init]{regulog_shiny_init()}}.}

\item{reader}{A function that reads data, e.g. \code{haven::read_sas},
\code{readr::read_csv}, \code{data.table::fread}, \code{utils::read.csv}.}

\item{...}{Arguments passed to \code{reader}.}
}
\value{
The result of calling \code{reader(...)}.
}
\description{
Calls \code{reader} with \code{...}, then records the call as a \code{data_read} ACTION
entry. Unlike namespace patching, \code{rl_read()} wraps the call explicitly —
no package internals are modified, and behaviour is identical whether
called from a single script or concurrently across multiple Shiny
sessions.
}
\details{
The path/file recorded in the audit entry is resolved as follows:
\enumerate{
\item A named argument in \code{...} called \code{file}, \code{path}, or \code{data_file}.
\item The first unnamed argument in \code{...}, if any.
\item \code{"unknown"}, if neither is found.
}

This avoids the failure mode of positional-only extraction, where a
reordered named call (e.g. \code{read_csv(col_types = "ccd", file = "x.csv")})
would otherwise record the wrong value.
}
\examples{
log <- regulog_init(app = "pipeline", version = "1.0", user = "jsmith")

\dontrun{
adsl <- rl_read(log, haven::read_sas, "data/adsl.sas7bdat")
adae <- rl_read(log, readr::read_csv, file = "data/adae.csv")
}

}
\seealso{
\code{\link[=with_log]{with_log()}}
}
