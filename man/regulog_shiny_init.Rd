% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{regulog_shiny_init}
\alias{regulog_shiny_init}
\title{Initialise a regulog session inside a Shiny server}
\usage{
regulog_shiny_init(
  session,
  app,
  version = "unknown",
  path = NULL,
  hash_algo = "sha256"
)
}
\arguments{
\item{session}{The Shiny \code{session} object.}

\item{app}{Character. Application name.}

\item{version}{Character. Application version.}

\item{path}{Character or \code{NULL}. Persistent log file path. When \code{NULL},
a per-session temp file is created (suitable for development only;
logs will be lost when the session ends).}

\item{hash_algo}{Character. Hashing algorithm. Defaults to \code{"sha256"}.}
}
\value{
A \code{regulog} object with the log tied to the authenticated session
user.
}
\description{
A thin wrapper around \code{\link[=regulog_init]{regulog_init()}} that resolves the authenticated user
from \code{session$user} (set by Shiny Server Pro / Posit Connect) and
automatically logs \code{session_start} and \code{session_end} events.
}
\details{
\subsection{User resolution}{

\code{session$user} is the authenticated identity set by Shiny Server Pro or
Posit Connect. In open deployments where authentication is not configured,
this will be \code{NULL} or \code{""}. \code{regulog_shiny_init()} falls back to
\code{Sys.info()[["user"]]} in that case, with a warning.
}

\subsection{Session instrumentation}{

Two entries are added automatically:
\itemize{
\item \code{session_start} when \code{regulog_shiny_init()} is called
\item \code{session_end} via \code{shiny::onSessionEnded()}
}

These bracket all user-driven entries, giving regulators a complete
picture of each session lifecycle.
}

\subsection{Recommended pattern}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{server <- function(input, output, session) \{

  log <- regulog_shiny_init(
    session = session,
    app     = "my-app",
    version = "1.2.0",
    path    = "/logs/audit.rlog"
  )

  observeEvent(input$approve, \{
    log_action(log,
      action = "approved",
      object = input$dataset,
      reason = input$reason
    )
  \})
\}
}\if{html}{\out{</div>}}
}
}
\examples{
\dontrun{
library(shiny)
library(regulog)

server <- function(input, output, session) {
  log <- regulog_shiny_init(
    session = session,
    app     = "my-app",
    version = "1.0.0",
    path    = "logs/audit.rlog"
  )
  observeEvent(input$submit, {
    log_action(log,
      action = "submitted",
      object = input$form_id,
      reason = input$justification
    )
  })
}

shinyApp(ui = fluidPage(), server = server)
}

}
