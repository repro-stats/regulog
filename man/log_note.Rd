% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_note.R
\name{log_note}
\alias{log_note}
\title{Log a free-text note in the audit trail}
\usage{
log_note(log, text)
}
\arguments{
\item{log}{A \code{regulog} object returned by \code{\link[=regulog_init]{regulog_init()}} or
\code{\link[=regulog_shiny_init]{regulog_shiny_init()}}.}

\item{text}{The note text. Cannot be blank or whitespace-only.}
}
\value{
The \code{regulog} object, invisibly (pipe-friendly).
}
\description{
Records a NOTE entry — a free-text annotation that adds context, intent, or
an observation without requiring a discrete action verb or a before/after
value. Use it to document analytical decisions, assumptions, or rationale
that do not fit \code{\link[=log_action]{log_action()}} or \code{\link[=log_change]{log_change()}}.
}
\details{
Like all \code{regulog} entries, \code{text} is mandatory with no default and is
included in the hash chain, making it tamper-evident.
}
\examples{
log <- regulog_init(app = "analysis", version = "1.0", user = "ndoh.penn")

log_note(log, "Baseline window defined as Day -1 to Day 1 per protocol v3 §5.2")
log_note(log, "Outlier in subject 042 discussed with medical monitor — retained per SAP")

}
\seealso{
\code{\link[=log_action]{log_action()}}, \code{\link[=log_change]{log_change()}}, \code{\link[=log_signature]{log_signature()}}
}
