% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_audit_trail}
\alias{export_audit_trail}
\title{Export the audit trail}
\usage{
export_audit_trail(
  log,
  format = c("csv", "json"),
  from = NULL,
  to = NULL,
  path = NULL,
  signed = FALSE,
  include_genesis = FALSE
)
}
\arguments{
\item{log}{A \code{regulog} object or a path to a \code{.rlog} file.}

\item{format}{Character. \code{"csv"} or \code{"json"}.}

\item{from}{Character or \code{NULL}. Include entries on or after this date
(ISO-8601, e.g. \code{"2026-01-01"}).}

\item{to}{Character or \code{NULL}. Include entries on or before this date.}

\item{path}{Character or \code{NULL}. Output file path. If \code{NULL}, returns
the data without writing to disk.}

\item{signed}{Logical. If \code{TRUE}, verify the chain and include
\code{chain_intact} and \code{verified_at} fields in the export.}

\item{include_genesis}{Logical. Include the genesis record. Default \code{FALSE}.}
}
\value{
A data frame (CSV) or list (JSON), invisibly.
}
\description{
Serialises log entries to CSV or JSON, with optional date filtering. Use
\code{signed = TRUE} to run chain verification and stamp the integrity result
into the export — useful for handoffs, audits, or archival.
}
\details{
\subsection{CSV column layout}{\tabular{ll}{
   Column \tab Description \cr
   \code{entry_id} \tab Monotone sequence number \cr
   \code{timestamp} \tab ISO-8601 UTC \cr
   \code{app} \tab Application name \cr
   \code{app_version} \tab Application version \cr
   \code{user} \tab Acting user identity \cr
   \code{type} \tab \code{ACTION}, \code{CHANGE}, \code{NOTE}, or \code{SIGNATURE} \cr
   \code{action} \tab Action label (\code{ACTION} entries) \cr
   \code{object} \tab Target of the action or change \cr
   \code{field} \tab Field name (\code{CHANGE} entries) \cr
   \code{before} \tab Prior value (\code{CHANGE} entries) \cr
   \code{after} \tab New value (\code{CHANGE} and \code{SIGNATURE} entries) \cr
   \code{reason} \tab Justification (\code{ACTION}, \code{CHANGE}, \code{NOTE} entries) \cr
   \code{text} \tab Free-text annotation (\code{NOTE} entries) \cr
   \code{meaning} \tab Signature meaning (\code{SIGNATURE} entries) \cr
   \code{entry_hash} \tab SHA-256 of this entry \cr
   \code{prev_hash} \tab SHA-256 of prior entry \cr
   \code{chain_intact} \tab \code{TRUE}/\code{FALSE} (signed exports only) \cr
   \code{verified_at} \tab ISO-8601 UTC of export (signed exports only) \cr
}

}
}
\examples{
log <- regulog_init(app = "my-app", user = "jsmith")
log_action(log,
  action = "approved",
  object = "model_v3",
  reason = "Metrics passed threshold"
)
df <- export_audit_trail(log, format = "csv")

\donttest{
export_audit_trail(log,
  format = "csv",
  from   = "2026-01-01",
  signed = TRUE,
  path   = tempfile(fileext = ".csv")
)
}

}
