% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_signature.R
\name{log_signature}
\alias{log_signature}
\title{Apply an electronic signature to the audit trail}
\usage{
log_signature(log, meaning)
}
\arguments{
\item{log}{A \code{regulog} object returned by \code{\link[=regulog_init]{regulog_init()}} or
\code{\link[=regulog_shiny_init]{regulog_shiny_init()}}.}

\item{meaning}{The meaning of the signature — what you are certifying.
Cannot be blank. Example: \code{"I certify that this analysis is accurate and complete per SAP version 2.0"}.}
}
\value{
The \code{regulog} object, invisibly (pipe-friendly).
}
\description{
Records a SIGNATURE entry capturing the signer identity (from the session
user), UTC timestamp, number of prior entries covered, and the stated
meaning of the signature. Addresses 21 CFR Part 11 §11.100 / §11.200
requirements:
}
\details{
\itemize{
\item \strong{Identity} — resolved from the session user set in \code{\link[=regulog_init]{regulog_init()}}
\item \strong{Date and time} — UTC timestamp generated automatically at call time
\item \strong{Meaning} — the \code{meaning} argument; mandatory, cannot be blank
\item \strong{Coverage} — number of prior entries in the session, recorded automatically
}

The SIGNATURE entry is part of the hash chain: any tampering with entries
preceding the signature, or with the signature entry itself, is detectable
by \code{\link[=verify_log]{verify_log()}}.
}
\examples{
log <- regulog_init(app = "analysis", version = "1.0", user = "ndoh.penn")
log_action(log, "run", "primary_analysis.R",
           "Primary ANCOVA model executed per SAP section 6.1")

log_signature(log,
  "I certify that this analysis is accurate and complete per SAP version 2.0"
)

}
\seealso{
\code{\link[=log_action]{log_action()}}, \code{\link[=log_note]{log_note()}}, \code{\link[=verify_log]{verify_log()}}
}
