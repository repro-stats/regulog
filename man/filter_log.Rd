% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_log.R
\name{filter_log}
\alias{filter_log}
\title{Filter audit log entries}
\usage{
filter_log(
  log,
  type = NULL,
  user = NULL,
  action = NULL,
  from = NULL,
  to = NULL
)
}
\arguments{
\item{log}{A \code{regulog} object \strong{or} a path to a \code{.rlog} file.}

\item{type}{Character vector of entry types to keep: \code{"ACTION"},
\code{"CHANGE"}, \code{"NOTE"}, \code{"SIGNATURE"}. \code{NULL} returns all types.}

\item{user}{Character vector of user identifiers to keep. \code{NULL} returns
all users.}

\item{action}{Character vector of action values to keep (e.g.
\code{"approved"}, \code{"data_read"}). \code{NULL} returns all actions.}

\item{from}{Start of the time window. ISO 8601 string (\code{"2026-06-01"})
or \code{Date}. \code{NULL} applies no lower bound.}

\item{to}{End of the time window. Same format as \code{from}. Inclusive.
\code{NULL} applies no upper bound.}
}
\value{
A \code{data.frame} of matching entries, sorted by \code{entry_id}.
Returns a zero-row data frame when nothing matches.
}
\description{
Extracts a subset of entries from a \code{regulog} object or a \code{.rlog} file
as a plain \code{data.frame}. All filter arguments are optional — omitting all
returns every entry.
}
\examples{
log <- regulog_init(app = "analysis", version = "1.0", user = "jsmith")
log_action(log,
  action = "run",
  object = "primary.R",
  reason = "Primary model fitted"
)
log_note(log, "Outlier in subject 042 retained per SAP")
log_action(log,
  action = "export",
  object = "results.csv",
  reason = "Sent to sponsor"
)
log_signature(log, "Analysis complete and accurate per SAP v2")

# All entries as a data frame
filter_log(log)

# Only signatures
filter_log(log, type = "SIGNATURE")

# Actions and notes by a specific user
filter_log(log, type = c("ACTION", "NOTE"), user = "jsmith")

# Entries within a date range
filter_log(log, from = "2026-06-01", to = "2026-12-31")

# Works directly on a .rlog file — no live session needed
\donttest{
tmp <- tempfile(fileext = ".rlog")
log2 <- regulog_init(app = "analysis", version = "1.0", user = "jsmith",
  path = tmp)
log_action(log2,
  action = "run",
  object = "primary.R",
  reason = "Primary model fitted"
)
filter_log(tmp, type = "ACTION")
}

}
\seealso{
\code{\link[=as.data.frame.regulog]{as.data.frame.regulog()}}, \code{\link[=export_audit_trail]{export_audit_trail()}}, \code{\link[=verify_log]{verify_log()}}
}
