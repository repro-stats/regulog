% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{with_log}
\alias{with_log}
\title{Run an expression with automatic data read logging}
\usage{
with_log(log, expr)
}
\arguments{
\item{log}{A \code{regulog} object.}

\item{expr}{An expression, typically a \code{{}} block. Inside the block,
\code{read(reader, ...)} is available and logs to \code{log} automatically.}
}
\value{
The value of \code{expr}, invisibly.
}
\description{
Evaluates \code{expr} with a local \code{read()} binding tied to \code{log}, so calls
inside the block don't need to repeat the \code{log} argument. Reads must use
\code{read()} explicitly inside the block; calling a reader function directly
(e.g. bare \code{haven::read_sas(...)}) is not logged. This keeps logging
coverage unambiguous: every logged read is visible at the call site, and
there are no implicit gaps.
}
\examples{
log <- regulog_init(app = "pipeline", version = "1.0", user = "jsmith")

\dontrun{
with_log(log, {
  adsl <- read(haven::read_sas, "data/adsl.sas7bdat")
  adae <- read(haven::read_sas, "data/adae.sas7bdat")
})

filter_log(log, action = "data_read")
}

}
\seealso{
\code{\link[=rl_read]{rl_read()}}
}
